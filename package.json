{
  "name": "think-alchemist",
  "version": "1.0.0",
  "description": "Think Alchemist - Full Stack Application",
  "scripts": {
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.x"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.84.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "groq-sdk": "^0.3.3",
    "node-fetch": "^2.7.0",
    "replicate": "^0.25.2"
  }
}

